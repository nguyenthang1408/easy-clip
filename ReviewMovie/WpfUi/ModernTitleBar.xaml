<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="White"
             d:DesignHeight="64" d:DesignWidth="1200">
  <UserControl.Resources>
    <SolidColorBrush x:Key="Border" Color="#E5E7EB" />
    <SolidColorBrush x:Key="Accent" Color="#F97316" />
    <SolidColorBrush x:Key="TextMuted" Color="#6B7280" />

    <Style x:Key="WinBtn" TargetType="{x:Type Button}">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="Foreground" Value="#111827" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Width" Value="52" />
      <Setter Property="Height" Value="36" />
      <Setter Property="FontSize" Value="16" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border Background="{TemplateBinding Background}" CornerRadius="8">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#F3F4F6"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="CloseBtn" TargetType="{x:Type Button}" BasedOn="{StaticResource WinBtn}">
      <Style.Triggers>
        <Trigger Property="IsMouseOver" Value="True">
          <Setter Property="Background" Value="#FEE2E2"/>
          <Setter Property="Foreground" Value="#991B1B"/>
        </Trigger>
      </Style.Triggers>
    </Style>

    <Style x:Key="IconBtn" TargetType="{x:Type Button}" BasedOn="{StaticResource WinBtn}">
      <Setter Property="Width" Value="36" />
      <Setter Property="Height" Value="36" />
      <Setter Property="FontSize" Value="14" />
    </Style>
  </UserControl.Resources>

  <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Background="White">
    <Grid Height="64">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <!-- Drag area -->
      <Grid x:Name="DragArea" Grid.Column="0" Background="Transparent" Margin="10,0,0,0">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
          <Border Width="30" Height="30" CornerRadius="8" Background="{StaticResource Accent}">
            <TextBlock Text="E" Foreground="White" FontWeight="Bold" FontSize="16"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
          </Border>

          <StackPanel Margin="12,0,0,0">
            <TextBlock Text="EasyClip Studio" FontWeight="SemiBold" FontSize="16" Foreground="#111827" />
            <TextBlock Text="TPMEDIA PREMIUM" FontSize="12" Foreground="{StaticResource TextMuted}" />
          </StackPanel>
        </StackPanel>
      </Grid>

      <!-- Window buttons -->
      <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,10,0">
        <!-- status -->
        <Border Background="#ECFDF3" CornerRadius="10" Padding="8,3" Margin="0,0,10,0">
          <TextBlock Text="Active" Foreground="#16A34A" FontSize="12" FontWeight="SemiBold"/>
        </Border>

        <TextBlock Text="1 Day Remaining" Foreground="{StaticResource TextMuted}" FontSize="12"
                   VerticalAlignment="Center" Margin="0,0,14,0"/>

        <!-- icons -->
        <Button x:Name="BtnHelp" Style="{StaticResource IconBtn}" Content="?" Margin="0,0,2,0"/>
        <Button x:Name="BtnSettings" Style="{StaticResource IconBtn}" Content="⚙" Margin="0,0,8,0"/>

        <!-- avatar -->
        <Border Width="34" Height="34" CornerRadius="17" Background="{StaticResource Accent}" Margin="0,0,10,0">
          <TextBlock Text="TN" Foreground="White" FontWeight="Bold" FontSize="12"
                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>

        <Button x:Name="BtnMinimize" Style="{StaticResource WinBtn}" Content="—" />
        <Button x:Name="BtnMaximize" Style="{StaticResource WinBtn}" Content="▢" />
        <Button x:Name="BtnClose" Style="{StaticResource CloseBtn}" Content="✕" />
      </StackPanel>
    </Grid>
  </Border>
</UserControl>

