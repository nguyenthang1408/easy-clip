<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#F4F7FB"
             d:DesignHeight="720" d:DesignWidth="360">
  <UserControl.Resources>
    <SolidColorBrush x:Key="CardBg" Color="White" />
    <SolidColorBrush x:Key="TextMuted" Color="#6B7280" />
    <SolidColorBrush x:Key="Accent" Color="#F97316" />
    <SolidColorBrush x:Key="Border" Color="#E5E7EB" />

    <DropShadowEffect x:Key="CardShadow" BlurRadius="18" ShadowDepth="2" Opacity="0.15" />

    <Style x:Key="CardStyle" TargetType="{x:Type Border}">
      <Setter Property="Background" Value="{StaticResource CardBg}" />
      <Setter Property="CornerRadius" Value="14" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="BorderBrush" Value="{StaticResource Border}" />
      <Setter Property="Effect" Value="{StaticResource CardShadow}" />
      <Setter Property="Padding" Value="14" />
      <Setter Property="Margin" Value="8,8,8,8" />
      <Setter Property="SnapsToDevicePixels" Value="True" />
    </Style>

    <Style x:Key="PrimaryButton" TargetType="{x:Type Button}">
      <Setter Property="Background" Value="{StaticResource Accent}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="Height" Value="42" />
      <Setter Property="MinWidth" Value="96" />
      <Setter Property="Padding" Value="18,0" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="FontSize" Value="14" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border Background="{TemplateBinding Background}"
                    CornerRadius="10"
                    SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="GhostButton" TargetType="{x:Type Button}">
      <Setter Property="Background" Value="#F3F4F6" />
      <Setter Property="Foreground" Value="#111827" />
      <Setter Property="Height" Value="40" />
      <Setter Property="MinWidth" Value="88" />
      <Setter Property="Padding" Value="16,0" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="FontWeight" Value="SemiBold" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Button}">
            <Border Background="{TemplateBinding Background}"
                    CornerRadius="10"
                    SnapsToDevicePixels="True">
              <ContentPresenter HorizontalAlignment="Center"
                                VerticalAlignment="Center" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ModernTextBox" TargetType="{x:Type TextBox}">
      <Setter Property="BorderBrush" Value="{StaticResource Border}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Background" Value="#FBFDFF" />
      <Setter Property="Padding" Value="10,8" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type TextBox}">
            <Border x:Name="Bd"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="12"
                    SnapsToDevicePixels="True">
              <Grid>
                <ScrollViewer x:Name="PART_ContentHost" />
              </Grid>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.55" />
              </Trigger>
              <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="#60A5FA" />
                <Setter TargetName="Bd" Property="Background" Value="#FFFFFF" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="#CBD5E1" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="ModernComboBox" TargetType="{x:Type ComboBox}">
      <Setter Property="SnapsToDevicePixels" Value="True" />
      <Setter Property="Background" Value="#FBFDFF" />
      <Setter Property="BorderBrush" Value="{StaticResource Border}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="10,8" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Foreground" Value="#111827" />
      <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
      <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ComboBox}">
            <Grid>
              <Border x:Name="Bd"
                      Background="{TemplateBinding Background}"
                      BorderBrush="{TemplateBinding BorderBrush}"
                      BorderThickness="{TemplateBinding BorderThickness}"
                      CornerRadius="12"
                      SnapsToDevicePixels="True">
                <Grid>
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="34" />
                  </Grid.ColumnDefinitions>

                  <ContentPresenter Margin="{TemplateBinding Padding}"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Left"
                                    Content="{Binding SelectionBoxItem, RelativeSource={RelativeSource TemplatedParent}}"
                                    ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                    ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                    RecognizesAccessKey="True" />

                  <ToggleButton Grid.Column="1"
                                Focusable="False"
                                IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                                Background="Transparent"
                                BorderThickness="0"
                                Cursor="Hand">
                    <Grid>
                      <Path Data="M 0 0 L 6 6 L 12 0"
                            Stroke="#6B7280"
                            StrokeThickness="2"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            StrokeLineJoin="Round" />
                    </Grid>
                  </ToggleButton>
                </Grid>
              </Border>

              <Popup x:Name="PART_Popup"
                     AllowsTransparency="True"
                     Focusable="False"
                     Placement="Bottom"
                     IsOpen="{TemplateBinding IsDropDownOpen}"
                     PopupAnimation="Fade">
                <Grid SnapsToDevicePixels="True" MinWidth="{TemplateBinding ActualWidth}">
                  <Border Background="White"
                          BorderBrush="#E5E7EB"
                          BorderThickness="1"
                          CornerRadius="12"
                          Padding="6"
                          SnapsToDevicePixels="True">
                    <ScrollViewer Margin="0" SnapsToDevicePixels="True">
                      <ItemsPresenter />
                    </ScrollViewer>
                  </Border>
                </Grid>
              </Popup>
            </Grid>

            <ControlTemplate.Triggers>
              <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.55" />
              </Trigger>
              <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="#60A5FA" />
                <Setter TargetName="Bd" Property="Background" Value="#FFFFFF" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="BorderBrush" Value="#CBD5E1" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
      <Setter Property="ItemContainerStyle">
        <Setter.Value>
          <Style TargetType="{x:Type ComboBoxItem}">
            <Setter Property="Padding" Value="10,8" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Template">
              <Setter.Value>
                <ControlTemplate TargetType="{x:Type ComboBoxItem}">
                  <Border x:Name="ItemBd" Background="{TemplateBinding Background}" CornerRadius="10">
                    <ContentPresenter />
                  </Border>
                  <ControlTemplate.Triggers>
                    <Trigger Property="IsHighlighted" Value="True">
                      <Setter TargetName="ItemBd" Property="Background" Value="#F3F4F6" />
                    </Trigger>
                    <Trigger Property="IsSelected" Value="True">
                      <Setter TargetName="ItemBd" Property="Background" Value="#E0F2FE" />
                    </Trigger>
                  </ControlTemplate.Triggers>
                </ControlTemplate>
              </Setter.Value>
            </Setter>
          </Style>
        </Setter.Value>
      </Setter>
    </Style>

    <Style x:Key="SectionTitle" TargetType="{x:Type TextBlock}">
      <Setter Property="FontSize" Value="12" />
      <Setter Property="FontWeight" Value="Bold" />
      <Setter Property="Foreground" Value="#111827" />
    </Style>

    <Style x:Key="FieldLabel" TargetType="{x:Type TextBlock}">
      <Setter Property="FontSize" Value="12" />
      <Setter Property="Foreground" Value="#374151" />
      <Setter Property="Margin" Value="0,0,0,6" />
    </Style>

    <Style TargetType="{x:Type CheckBox}">
      <Setter Property="Foreground" Value="#111827" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type CheckBox}">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
              <Border x:Name="Box"
                      Width="18" Height="18"
                      CornerRadius="6"
                      Background="White"
                      BorderBrush="{StaticResource Border}"
                      BorderThickness="1"
                      Margin="0,0,8,0">
                <Path x:Name="Tick"
                      Data="M 2 9 L 7 14 L 16 4"
                      Stroke="White"
                      StrokeThickness="2"
                      StrokeLineJoin="Round"
                      Visibility="Collapsed" />
              </Border>
              <ContentPresenter VerticalAlignment="Center" />
            </StackPanel>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Box" Property="Background" Value="#F97316" />
                <Setter TargetName="Box" Property="BorderThickness" Value="0" />
                <Setter TargetName="Tick" Property="Visibility" Value="Visible" />
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Box" Property="BorderBrush" Value="#CBD5E1" />
              </Trigger>
              <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.55" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </UserControl.Resources>

  <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
    <StackPanel>
      <!-- Project -->
      <Border Style="{StaticResource CardStyle}">
        <StackPanel>
          <TextBlock Style="{StaticResource SectionTitle}" Text="Cài đặt / Dự án" />
          <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="CbProject" Style="{StaticResource ModernComboBox}" />
            <Button x:Name="BtnCreateProject" Grid.Column="2" Style="{StaticResource GhostButton}" Content="Create" />
          </Grid>
        </StackPanel>
      </Border>

      <!-- Voice -->
      <Border Style="{StaticResource CardStyle}">
        <StackPanel>
          <TextBlock Style="{StaticResource SectionTitle}" Text="Cài đặt Voice" />
          <TextBlock Margin="0,2,0,0" Foreground="{StaticResource TextMuted}" FontSize="11"
                     Text="Nguồn voice, ApiKey/Token." />

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="Nguồn Voice" />
          <Grid>
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="CbVoiceSource" Style="{StaticResource ModernComboBox}" />
            <Button x:Name="BtnSaveVoice" Grid.Column="2" Style="{StaticResource GhostButton}" Content="Save" />
          </Grid>

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="ApiKey / AppID" />
          <TextBox x:Name="TxtApiKey" Style="{StaticResource ModernTextBox}" />

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="Token (nếu có)" />
          <TextBox x:Name="TxtToken" Style="{StaticResource ModernTextBox}" />
        </StackPanel>
      </Border>

      <!-- Effects -->
      <Border Style="{StaticResource CardStyle}">
        <StackPanel>
          <TextBlock Style="{StaticResource SectionTitle}" Text="Lựa chọn hiệu ứng" />

          <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
              <TextBlock Style="{StaticResource FieldLabel}" Text="% ZoomUp" />
              <ComboBox x:Name="CbZoomUp" Style="{StaticResource ModernComboBox}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
              <TextBlock Style="{StaticResource FieldLabel}" Text="% ZQuality" />
              <ComboBox x:Name="CbZQuality" Style="{StaticResource ModernComboBox}" />
            </StackPanel>
          </Grid>

          <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
              <TextBlock Style="{StaticResource FieldLabel}" Text="FPS (Input)" />
              <TextBox x:Name="TxtFps" Style="{StaticResource ModernTextBox}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
              <TextBlock Style="{StaticResource FieldLabel}" Text="Thread" />
              <TextBox x:Name="TxtThread" Style="{StaticResource ModernTextBox}" />
            </StackPanel>
          </Grid>

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="Chất lượng" />
          <ComboBox x:Name="CbQuality" Style="{StaticResource ModernComboBox}" />

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="Lựa chọn" />
          <ComboBox x:Name="CbMode" Style="{StaticResource ModernComboBox}" />

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="Hiệu ứng" />
          <ComboBox x:Name="CbEffect" Style="{StaticResource ModernComboBox}" />

          <WrapPanel Margin="0,10,0,0">
            <CheckBox x:Name="ChkZoom" Margin="0,0,14,8" Content="Zoom" />
            <CheckBox x:Name="ChkRotate" Margin="0,0,14,8" Content="Xoay" />
            <CheckBox x:Name="ChkFlip" Margin="0,0,14,8" Content="Lật" />
            <CheckBox x:Name="ChkFlipRandom" Margin="0,0,14,8" Content="Lật random" />
            <CheckBox x:Name="ChkMoveLR" Margin="0,0,14,8" Content="Di chuyển trái↔phải" />
            <CheckBox x:Name="ChkOpenPlayer" Margin="0,0,14,8" Content="Tự động mở player" />
          </WrapPanel>

          <Grid Margin="0,6,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
              <TextBlock Style="{StaticResource FieldLabel}" Text="Volumn gốc" />
              <TextBox x:Name="TxtVolume" Style="{StaticResource ModernTextBox}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
              <TextBlock Style="{StaticResource FieldLabel}" Text="Tốc độ đọc" />
              <TextBox x:Name="TxtSpeechSpeed" Style="{StaticResource ModernTextBox}" />
            </StackPanel>
          </Grid>

          <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
              <TextBlock Style="{StaticResource FieldLabel}" Text="Audio scale (from)" />
              <TextBox x:Name="TxtAudioScaleFrom" Style="{StaticResource ModernTextBox}" />
            </StackPanel>

            <StackPanel Grid.Column="2">
              <TextBlock Style="{StaticResource FieldLabel}" Text="Audio scale (to)" />
              <TextBox x:Name="TxtAudioScaleTo" Style="{StaticResource ModernTextBox}" />
            </StackPanel>

            <StackPanel Grid.Column="4">
              <TextBlock Style="{StaticResource FieldLabel}" Text="Ko dùng Audio" />
              <CheckBox x:Name="ChkMuted" VerticalAlignment="Center" />
            </StackPanel>
          </Grid>

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="Ngôn ngữ" />
          <ComboBox x:Name="CbLanguage" Style="{StaticResource ModernComboBox}" />

          <TextBlock Margin="0,10,0,0" Style="{StaticResource FieldLabel}" Text="Giọng đọc" />
          <ComboBox x:Name="CbVoice" Style="{StaticResource ModernComboBox}" />

          <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="*" />
              <ColumnDefinition Width="10" />
              <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
              <TextBlock Style="{StaticResource FieldLabel}" Text="Cấu hình" />
              <ComboBox x:Name="CbTemplate" Style="{StaticResource ModernComboBox}" />
            </StackPanel>
            <Button x:Name="BtnSaveEffect" Grid.Column="2" Style="{StaticResource GhostButton}" Content="Save" VerticalAlignment="Bottom" />
          </Grid>
        </StackPanel>
      </Border>

      <!-- Publish -->
      <Border Style="{StaticResource CardStyle}">
        <StackPanel>
          <TextBlock Style="{StaticResource SectionTitle}" Text="Xuất bản video" />
          <Button x:Name="BtnMergeSegments" Margin="0,12,0,0" Style="{StaticResource PrimaryButton}" Content="GHÉP CÁC ĐOẠN" />
          <TextBlock x:Name="TxtStatus" Margin="0,10,0,0" Foreground="{StaticResource TextMuted}" FontSize="11" Text="..." />
        </StackPanel>
      </Border>
    </StackPanel>
  </ScrollViewer>
</UserControl>

